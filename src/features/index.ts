export { default as sidebarReducer } from "features/sidebar/sidebarSlice";
export { default as catsReducer } from "features/cats/catsSlice";
export { default as appReducer } from "features/app/appSlice";

// Not sure how correct this is I usually don't use default imports. But its much better than having x10 imports in store.
